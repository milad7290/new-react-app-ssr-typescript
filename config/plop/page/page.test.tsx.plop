import * as React from 'react';
import { shallow } from 'enzyme';

import {{pascalCase pageName}} from './{{pascalCase pageName}}';

describe('{{pascalCase pageName}}', () => {
    const defaultProps = {};

    it('tests something', () => {
        shallow(<{{pascalCase pageName}} {...defaultProps} />);
    });
});
