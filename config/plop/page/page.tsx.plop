import * as React from 'react';
import { useTranslation } from 'react-i18next';

const {{pascalCase pageName}} : React.FC<any> = () => {
    const { t } = useTranslation();

    return (<React.Fragment>
    <h2>{t('features')}</h2>

</React.Fragment>);
};

export default {{pascalCase pageName}};
